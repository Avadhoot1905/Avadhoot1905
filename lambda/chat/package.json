{
  "name": "chat-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda for Gemini AI chat functionality",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm install && npm run build && zip -r function.zip ."
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@upstash/redis": "^1.35.6",
    "@prisma/client": "^6.17.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.5",
    "prisma": "^6.17.1",
    "typescript": "^5.7.3"
  }
}
