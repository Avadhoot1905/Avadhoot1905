{
  "name": "portfolio-lambda",
  "version": "1.0.0",
  "description": "Consolidated AWS Lambda for chat and admin functionality",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist deploy",
    "build": "esbuild index.ts --bundle --platform=node --target=node22 --format=cjs --minify --outfile=dist/index.js",
    "deploy:build": "npm run clean && npm install && npx prisma generate && npm run build && npm run deploy:package",
    "deploy:package": "mkdir -p deploy && cp -r dist node_modules prisma package.json deploy/ && cd deploy && zip -r lambda.zip . && cd .. && echo '\n✅ Deployment package created: lambda/deploy/lambda.zip'",
    "zip": "cd dist && zip -r deploy.zip index.js",
    "deploy:prep": "rm -rf dist && mkdir dist && npm run build && cd dist && zip -r deploy.zip index.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@upstash/redis": "^1.35.6",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.1",
    "express": "^5.2.1",
    "pg": "^8.19.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/express": "^5.0.6",
    "@types/node": "^22.10.5",
    "drizzle-kit": "^0.31.9",
    "esbuild": "^0.27.3",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
